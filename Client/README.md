React client scaffold (Vite)

Run locally:

```powershell
cd Client
npm install
npm run dev
```

Open the URL printed by `vite` (usually http://localhost:5173).

Files:
- `index.html` - app entry
- `src/main.jsx` - mount point
- `src/App.jsx` - basic App component
